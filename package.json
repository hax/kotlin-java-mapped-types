{
  "name": "kotlin-java-mapped-types",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "This project documents the mapped types between Kotlin and Java as specified in the [Kotlin documentation](https://kotlinlang.org/docs/java-interop.html#mapped-types).",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "sync": "tsx lib/sync-resources.ts",
    "generate": "tsx lib/generate-all.ts",
    "generate:mapping-details": "tsx lib/generate-mapping-details.ts",
    "generate:mapped-types": "tsx lib/generate-mapped-types-yaml.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "cheerio": "^1.1.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "yaml": "^2.8.2"
  }
}
